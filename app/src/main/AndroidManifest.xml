<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecommercedemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceDemo"
        android:usesCleartextTraffic="true">
        <activity android:name=".all_products.searchProducts.SearchProducts">

        </activity>
        <activity android:name=".myCart.MyCart" />
        <activity android:name=".produckDetails.ProductDetails" />
        <activity android:name=".all_products.All_Products">

        </activity>
        <activity android:name=".homepage.HomePage">

        </activity>
        <activity android:name=".register.Register" />
        <activity android:name=".login.Login">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>